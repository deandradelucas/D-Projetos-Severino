# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (NUNCA fazer commit)
# .env.*.local cobre .env.production.local, .env.staging.local, etc.
.env
.env.local
.env.production
.env.*.local
src/lib/supabase_config.js

# Local tooling artifacts
.agent/
.agents/
.playwright-mcp/
skills-lock.json
nul

.vercel

# Backup temp
.backup-temp/
.tmp/
docker_logs.txt

# CSS fingerprint baseline (gerado localmente; depende do ambiente)
scripts/css-fingerprint/fingerprint-baseline.json
