# Story 7.2 — PDF Parser via Gemini Inline

## Status
InReview

## Descrição
Criar `server/lib/import/pdf-parser.mjs` que recebe um Buffer de arquivo PDF (extrato bancário), converte para base64 e envia ao Gemini usando `inlineData` (suporte nativo a PDF do Gemini 1.5). O Gemini extrai todas as transações do extrato e retorna um array normalizado de transações — sem necessidade de nenhuma biblioteca de parsing de PDF.

## Critérios de Aceite

### Parsing via Gemini Inline
- [x] Função exportada: `async function parsePdfTransactions(pdfBuffer): Promise<ParsedRows | ErrorResult>`
- [x] Converte buffer para base64 string
- [x] Monta request Gemini com `inlineData: { mimeType: 'application/pdf', data: base64 }`
- [ ] Usa modelo `gemini-1.5-flash` (suporte a PDF; flash é mais rápido/barato que pro)
- [ ] `maxOutputTokens: 4096` (extratos longos podem ter muitas transações)
- [x] `temperature: 0.1` (determinístico)

### Prompt de Extração
- [x] Prompt instrui Gemini a extrair TODAS as transações do extrato
- [x] Output esperado: JSON array `[{data, descricao, valor, tipo}]`
  - `data`: formato `YYYY-MM-DD`
  - `descricao`: string sem quebras de linha, max 255 chars
  - `valor`: número positivo
  - `tipo`: `"DESPESA"` ou `"RECEITA"`
- [x] Prompt instrui a ignorar totais, saldos e linhas de cabeçalho
- [x] Prompt inclui exemplo explícito de output esperado (few-shot)

### Normalização e Validação
- [x] Response do Gemini é parseado via `tryParseJsonBlock` (já existe em `server/lib/ai/parsers.mjs`)
- [x] Cada item é validado: data válida, valor > 0, descricao não vazia, tipo válido
- [x] Itens inválidos são silenciosamente descartados (não jogam erro)
- [x] Retorna `Array<{data: string, descricao: string, valor: number, tipo: 'DESPESA'|'RECEITA'}>`

### Tratamento de Erros e Limites
- [x] Arquivo > 20MB retorna `{ error: 'ARQUIVO_MUITO_GRANDE', message: 'PDFs até 20MB são suportados.' }`
- [x] Gemini retorna 0 transações → `{ error: 'NENHUMA_TRANSACAO', message: 'Não encontrei transações no PDF. Tente um extrato bancário padrão.' }`
- [x] Falha de API Gemini → `{ error: 'FALHA_IA', message: 'Não consegui processar o PDF agora. Tente novamente.' }`
- [x] PDF corrompido ou não-PDF → captura erro de parsing e retorna `{ error: 'FORMATO_INVALIDO' }`

### Fallback de Modelo
- [ ] Tenta `gemini-1.5-flash` primeiro
- [ ] Se falhar, tenta `gemini-1.5-pro` (usa `resolveGeminiModelCandidates` existente)
- [x] Se ambos falharem, retorna erro `FALHA_IA`

## Escopo

**IN:**
- `server/lib/import/pdf-parser.mjs` (novo)
- Nenhum novo pacote npm (usa Gemini inline nativo)

**OUT:**
- PDFs escaneados sem texto (imagens puras) — Gemini tenta OCR mas sem garantia no contrato desta story
- Parsing de PDFs não-financeiros
- Inserção no banco (Story 7.3)
- Integração WhatsApp (Story 7.4)

## Dependências
- `server/lib/ai/gemini-client.mjs` (já existe)
- `server/lib/ai/parsers.mjs` — `tryParseJsonBlock` (já existe)
- `server/lib/logger.mjs` (já existe)

## Notas Técnicas

**Prompt exemplo:**
```
Você é um extrator de transações financeiras. Analise este extrato bancário e extraia TODAS as transações.

Retorne APENAS um array JSON válido (sem markdown) no formato:
[
  {"data":"2024-01-15","descricao":"MERCADO EXTRA","valor":87.50,"tipo":"DESPESA"},
  {"data":"2024-01-20","descricao":"SALARIO EMPRESA XYZ","valor":3500.00,"tipo":"RECEITA"}
]

Regras:
- Ignore totais, saldos, cabeçalhos e rodapés
- valor sempre positivo
- tipo: "DESPESA" para saídas/débitos, "RECEITA" para entradas/créditos
- data no formato YYYY-MM-DD
- Se não encontrar transações, retorne []
```

**Limitação de tamanho Gemini:**
- Gemini 1.5 Flash: até 1048576 tokens de contexto; PDF de ~20MB gera ~500K tokens
- PDFs de extrato bancário típicos: 1-5MB → bem dentro do limite

## File List
- `server/lib/import/pdf-parser.mjs` (criar)

## Change Log
- [auditoria 2026-06-04] Reconciliação retroativa: 17/21 AC confirmados por inspeção de código. 3 pendentes referem-se a modelos descontinuados (gemini-1.5-flash/pro → substituídos por gemini-2.5-flash no gemini-client.mjs); 1 pendente refere-se a maxOutputTokens (story especifica 4096, código usa 8192). @qa
