/** Corpo rolável abaixo do hero no shell — o cabeçalho em vidro permanece visível. */
export default function RefDashboardScroll({ children }) {
  return <div className="ref-dashboard-scroll">{children}</div>
}
